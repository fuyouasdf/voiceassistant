# Music Player TODO

本文档记录本机音乐播放器、播放页、队列页、歌词页的后续计划。
目标是把当前“功能已成型”的播放器继续推进到“交互完整、视觉统一、行为稳定”的状态。

## 当前状态

- 已支持播放设备选择：`本机` + 远程 Jellyfin 会话设备
- 本机播放已支持当前列表入队
- 已支持完整播放页、迷你播放栏、通知栏控制
- 已支持队列页、拖拽排序、删除队列项、清空队列
- 已支持歌词加载、滚动高亮、点击跳转、全屏歌词模式

## P0 核心完善

1. 为 `LyricsFullscreenActivity` 加入封面模糊背景
2. 为 `LyricsFullscreenActivity` 加入基于封面主色的渐变背景回退
3. 为无封面歌曲定义稳定的全屏歌词背景方案
4. 为歌词高亮行增加更精细的颜色层级区分
5. 为播放页的随机/循环按钮增加激活态图标变化，而不只是颜色变化
6. 为播放页的“更多”菜单补充当前歌词页入口一致性检查
7. 复查播放页在小屏设备上的布局压缩问题
8. 复查播放页在长歌名、长专辑名场景下的截断表现

## P1 通知栏与系统控制

9. 统一通知栏按钮图标风格，避免与页面按钮不一致
10. 检查通知栏点击后进入的页面是否总是落到正确的当前播放页
11. 复查锁屏媒体控制与应用内播放状态的一致性
12. 复查蓝牙耳机播放/暂停/上一首/下一首事件
13. 检查 MediaSession metadata 在切歌时是否总能及时更新
14. 检查通知栏在暂停、停止、切歌时的显示状态是否准确
15. 评估是否需要在通知栏中显示当前播放队列位置

## P1 歌词体验增强

16. 为歌词列表加入“手动滚动中”的视觉提示
17. 为全屏歌词页增加“锁屏常亮”开关
18. 为歌词点击跳转增加轻量反馈，例如当前行短暂强调
19. 为歌词加载失败增加明确错误提示，区分“无歌词”和“请求失败”
20. 评估是否需要支持纯文本歌词模式切换
21. 评估是否需要在歌词区显示时间轴标记
22. 评估是否需要支持横屏歌词阅读布局
23. 为歌词页增加状态恢复，返回后尽量保持当前阅读位置

## P1 队列与播放流程

24. 为队列页增加“上移/下移”按钮，兼容不方便拖拽的设备
25. 为队列页增加“下一首播放”插队能力
26. 为队列页增加当前歌曲之外的更多操作入口
27. 显示当前队列来源信息，例如来自浏览页或来自用户播放列表
28. 检查删除当前播放项后的切歌逻辑是否符合预期
29. 检查拖拽排序后 shuffle/repeat 状态是否仍然一致
30. 为清空队列增加确认步骤，避免误触
31. 评估是否需要保存上次本机播放队列

## P1 本机播放稳定性

32. 继续验证 WMA/ASF 强制转码场景的首播速度
33. 检查 seek 后进度上报与 Jellyfin 服务端状态是否一致
34. 检查切歌时歌词、封面、通知栏、MediaSession 是否同步更新
35. 检查连续快速点击切歌按钮时的播放器稳定性
36. 检查播放失败时 UI 提示是否足够明确
37. 检查播放结束自动下一首时队列页和歌词页的刷新一致性
38. 复查 `MusicPlayer` 在 shuffle + reorder + remove 组合场景下的行为

## P2 视觉统一

39. 统一播放页、队列页、全屏歌词页的卡片圆角和间距体系
40. 统一空状态文案和视觉样式
41. 统一高亮色使用，避免歌词、按钮、状态标签之间色彩漂移
42. 复查按钮点击区域，确保旧手机上也足够易点
43. 检查迷你播放栏与完整播放页的文案风格一致性
44. 复查播放页和队列页在深色背景下的对比度

## P2 可维护性与测试

45. 为歌词解析增加单元测试，覆盖 JSON 歌词和 LRC 歌词
46. 为队列重排与移除逻辑增加单元测试
47. 为本机播放列表入队逻辑增加测试
48. 为 `MusicPlayer` 的关键状态切换补充回归测试
49. 梳理 `NowPlayingActivity` 与 `LyricsFullscreenActivity` 的重复逻辑，评估抽取公共组件
50. 持续同步 `ARCHITECTURE_ANDROID.md`

## 建议执行顺序

1. 1
2. 9
3. 24
4. 32
5. 45

## 备注

- 后续若继续扩展播放器能力，可以把播放页、队列页、歌词页拆成独立的播放器专项设计文档。
- 所有影响行为的改动，都需要同步更新 `ARCHITECTURE_ANDROID.md`。
